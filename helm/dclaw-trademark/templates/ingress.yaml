apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "dclaw-trademark.name" . }}
  labels:
    app.kubernetes.io/name: {{ include "dclaw-trademark.name" . }}
spec:
  rules:
    - host: dclaw-trademark.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ include "dclaw-trademark.name" . }}
                port:
                  number: {{ .Values.frontend.service.port }}
